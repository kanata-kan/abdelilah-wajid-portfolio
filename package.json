{
  "name": "abdelilah-wajid-portfolio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@12.3.4",
  "engines": {
    "node": "24.20.0"
  },
  "scripts": {
    "predev": "node scripts/sync-runtime-contract.mjs --check && node scripts/prepare-layout.mjs --check && node scripts/verify-source-boundary.mjs",
    "dev": "next dev --hostname 127.0.0.1",
    "prebuild": "node scripts/sync-runtime-contract.mjs --check && node scripts/prepare-layout.mjs --check && node scripts/verify-source-boundary.mjs",
    "build": "next build",
    "start": "next start --hostname 127.0.0.1",
    "typecheck": "next typegen && tsc --noEmit",
    "lint": "eslint . --max-warnings=0",
    "check:content": "node scripts/verify-docs.mjs && node scripts/verify-runtime-assets.mjs && node scripts/sync-runtime-contract.mjs --check && node scripts/prepare-layout.mjs --check && node scripts/verify-source-boundary.mjs",
    "test": "node --test tests/foundation.test.ts",
    "test:smoke": "node scripts/smoke.mjs",
    "test:browser": "playwright test",
    "check": "pnpm typecheck && pnpm lint && pnpm check:content && pnpm test && pnpm build && pnpm test:smoke",
    "sync:contract": "node scripts/sync-runtime-contract.mjs && node scripts/prepare-layout.mjs",
    "check:contract": "node scripts/sync-runtime-contract.mjs --check && node scripts/prepare-layout.mjs --check && node scripts/verify-source-boundary.mjs"
  },
  "dependencies": {
    "next": "16.3.4",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "server-only": "0.0.1"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "@playwright/test": "1.63.0",
    "@types/node": "24.13.3",
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.7",
    "eslint": "9.39.5",
    "eslint-config-next": "16.3.4",
    "typescript": "6.0.3"
  }
}
